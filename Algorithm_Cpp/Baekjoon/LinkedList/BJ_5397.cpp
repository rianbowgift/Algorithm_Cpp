#include<iostream>
#include<list>
#include<string>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int a;
	cin >> a;

	for (int z = 0; z < a; z++) {

		string temp;
		cin >> temp;
		list<char> keylog_list;
		for (int i = 0; i < temp.size(); i++) {
			keylog_list.push_back(temp[i]);
		}

		

		







	}



	
}